<!DOCTYPE html>
<html lang="zh-CN">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>产品管理 - 欢迎界面</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-6 text-center">
        <h1 class="display-4">欢迎使用产品管理系统</h1>
        <p class="lead">在这里，您可以管理您的产品信息。</p>
        <% if(code === 0) { %>
          <div class="alert alert-warning" role="alert">
            <%= message %>
            <a href="/login" class="btn btn-secondary btn-lg me-3">登录</a>
            <a href="/register" class="btn btn-success btn-lg">注册</a>
          </div>
        <% } %>
        <% if(code === 1) { %>
          <a href="/products" class="btn btn-primary btn-lg">查看产品列表</a>
        <% } %>
      </div>
    </div>
  </div>
</body>

</html>