<!DOCTYPE html>
<html lang="zh-CN">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= response.product.name %></title>
  <!-- 引入Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
  <% if (!response.error){ %>
  <div class="container mt-5">
    <h1><%= response.product.name %></h1>
    <a href="/products/edit/<%= response.product.id %>" class="btn btn-info">修改</a>
    <p><%= response.product.description %></p>
    <p>价格: <%= response.product.price %> 元</p>
    <p>标签:
    <ul class="list-inline"></ul>
    <% response.product.tags.forEach(function(tag) { %>
    <span class="badge bg-secondary"><%= tag %></span>
    <% }) %>
    </ul>
    </p>
    <p>已删除: <%= response.product.deleted ? '是' : '否' %></p>
  </div>
  <% }else{ %>
    <div class="container mt-5">
        <h1>错误</h1>
        <p><%= response.error %></p>
    </div>
  <% } %>

  <!-- 引入Bootstrap 5 JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>